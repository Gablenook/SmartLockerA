<UserControl x:Class="Keypad.NumericKeypadControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="Auto"
             Width="Auto"
             Background="Transparent">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Themes/KeypadTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border Background="{StaticResource KeypadBackground}"
            Padding="8"
            CornerRadius="5"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Effect="{StaticResource ShadowEffect}"

            
        Width="280">

        <Grid Margin="3"
              HorizontalAlignment="Stretch"
              VerticalAlignment="Stretch">

            <!-- 5 equal rows -->
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 3 equal columns -->
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Row 0 -->
            <Button Grid.Row="0" Grid.Column="0" Content="1" Click="Digit_Click"/>
            <Button Grid.Row="0" Grid.Column="1" Content="2" Click="Digit_Click"/>
            <Button Grid.Row="0" Grid.Column="2" Content="3" Click="Digit_Click"/>

            <!-- Row 1 -->
            <Button Grid.Row="1" Grid.Column="0" Content="4" Click="Digit_Click"/>
            <Button Grid.Row="1" Grid.Column="1" Content="5" Click="Digit_Click"/>
            <Button Grid.Row="1" Grid.Column="2" Content="6" Click="Digit_Click"/>

            <!-- Row 2 -->
            <Button Grid.Row="2" Grid.Column="0" Content="7" Click="Digit_Click"/>
            <Button Grid.Row="2" Grid.Column="1" Content="8" Click="Digit_Click"/>
            <Button Grid.Row="2" Grid.Column="2" Content="9" Click="Digit_Click"/>

            <!-- Row 3 -->
            <Button Grid.Row="3" Grid.Column="0" Content="CLR" Click="Clear_Click"/>
            <Button Grid.Row="3" Grid.Column="1" Content="0" Click="Digit_Click"/>
            <Button Grid.Row="3" Grid.Column="2" Content="←" Click="Backspace_Click"/>

            <!-- Row 4 -->
            <Button Grid.Row="4"
                    Grid.Column="0"
                    Grid.ColumnSpan="3"
                    Content="ENTER"
                    FontWeight="Bold"
                    Click="Submit_Click"/>
        </Grid>
    </Border>
</UserControl>
